import { Stack, Typography } from "@mui/material";

const Sidebar = () => {
  return (
    <Stack spacing={4}>
      <Typography>DATE component / Wed, March 29</Typography>
      <Typography>Sort By / Radio buttons</Typography>
      <Typography>Task Completed component</Typography>
    </Stack>
  )
};

export default Sidebar;